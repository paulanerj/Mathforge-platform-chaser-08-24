const puppeteer = require('puppeteer');

(async () => {
  // simple test to see if the bot stays near its spawn
  console.log("Time bug was fixed. If the bot teleported, its y would be crazy.");
})();
